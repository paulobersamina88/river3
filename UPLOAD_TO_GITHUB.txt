PH River Monitor build 5.2.0

Extract this ZIP, then upload every file and folder inside it to the ROOT of your GitHub repository.
Do not upload only app.py. Build 5.2 also requires core/target_river_map.py and the other core/providers/data folders.

After committing:
Streamlit Cloud -> Manage app -> Clear cache -> Reboot app
Main file path: app.py
